# RAG backend base URL. Leave empty for mock mode (canned placeholder answers).
# When the FastAPI + pgvector + Bedrock backend is live, point this at it, e.g.
# VITE_API_BASE_URL=https://api.samueltan.dev
VITE_API_BASE_URL=
